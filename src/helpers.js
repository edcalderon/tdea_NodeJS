const hbs = require('hbs');

hbs.registerHelper('masInformación', () =>{
	console.log('Client-side code running');
	const button = document.getElementById('boton1');
	button.addEventListener('click', e => {
  	return descripcion1
	});
});